"""Evaluation layer — runs assignment test cases through the real pipeline
and checks actual behavior against documented expectations.

Expected-outcome checking lives here, never inside an agent or the
pipeline itself (see app/evaluation/runner.py)."""
