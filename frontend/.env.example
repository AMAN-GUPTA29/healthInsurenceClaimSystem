# Backend API base URL.
#
# Local dev: leave unset — src/services/api.ts falls back to
# http://localhost:8000 automatically.
#
# Production (Vercel): set this to the deployed Render backend URL, e.g.:
# VITE_API_BASE_URL=https://<render-backend-url>
VITE_API_BASE_URL=
